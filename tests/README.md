# tests

Cross-package integration and end-to-end tests live here from later phases
(`tests/integration`, `tests/e2e`). Unit tests stay next to their source in
each package (e.g. `apps/api/test`, `packages/shared/src`).

Empty in Phase 1.
