# scripts

Reserved for operational and experiment scripts introduced in later phases:

- `generate-failures.ts` — synthetic dataset generator (Phase 5)
- `run-baseline.ts` — naive retry baseline run (Phase 11)
- `run-agent.ts` — Recovery Desk experiment run (Phase 12)

Empty in Phase 1.
