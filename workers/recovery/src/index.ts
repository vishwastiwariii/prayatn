/**
 * Recovery worker — Phase 9.
 * Placeholder entry point: no queue, no jobs, no payment execution yet.
 */
function main(): void {
  process.stdout.write('[recovery-worker] placeholder — no jobs are processed in Phase 1\n');
}

main();
